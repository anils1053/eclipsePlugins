var ${className} = new Y.Base.create('${className}', ${yuiClassToExtends}, [], {
	initializer: function () {
	},

	destructor : function () {
	}
}, {
	ATTRS: {
	}
});

Y.namespace('${namespace}').${className} = ${className};
